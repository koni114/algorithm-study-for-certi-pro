dirs = "ULURRDLLU"
def solution(dirs):
    answer = 0
    def check_dir(start_node, direction):
        start_x, start_y = start_node
        if direction == "U":
            next_x, next_y = start_x, start_y + 1
        elif direction == "D":
            next_x, next_y = start_x, start_y - 1
        elif direction == "R":
            next_x, next_y = start_x + 1, start_y
        else:
            next_x, next_y = start_x - 1, start_y
            
        if -5 <= next_x, next_y <= 5
            
            
    
    return answer