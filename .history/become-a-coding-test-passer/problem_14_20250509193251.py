def solution(n, k, cmd):
    from collections import deque
    remove_row = deque([])
    check = ['O' for _ in range(n)]
    
    for cur_cmd in cmd:
        cur_cmd_list = cur_cmd.split(" ")
    
    
    
    answer = ''
    return answer