def solution(N, stages):
    stages = sorted(stages)
    answer = [0 for _ in range(len(N))]
    
    for 
    
    for idx, v in range(N):
        if idx >= 1 and stages[idx-1] != stages[idx]:
            
    
    
    
    return answer